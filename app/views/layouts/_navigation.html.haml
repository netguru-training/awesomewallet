= link_to "Awesomewallet", root_path, :class => 'brand'
%ul.nav
  - if signed_in?
    %li
      Logged in as #{current_user.name}
    %li
      = link_to 'Logout', logout_path
  - else
    %li
      = link_to 'Login', "#"
